# Port the server listens on (Railway sets this automatically in prod).
PORT=8787

# Henrik's Dev Valorant API key — grab one from https://discord.gg/henrikdev
# Without a key, /val/* endpoints respond with 503 "tracker not configured".
HENRIK_API_KEY=
